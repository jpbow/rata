import { mount } from 'enzyme'
import { create, act, ReactTestRenderer } from 'react-test-renderer'
import { {{properCase name}} } from '../'


it('should ', () => {
    const instance = create(<{{properCase name}}></{{properCase name}}>).root

    expect(instance).toBeDefined()
})
