{
    "files": [],
    "references": [
        // Core first because everything needs it
        { "path": "./packages/core" },

        // Components now
        { "path": "./packages/button" }
    ]
}
